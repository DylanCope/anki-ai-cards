# AGENTS.md — project conventions

Read by every loop iteration. Keep it short and current — this file and
PROGRESS.md are the only "memory" between iterations.

## Verification commands

<!-- The loop treats these as the definition of success. Update as the project
grows. Examples: -->

- Tests: `<e.g. pytest>` (not set up yet — first PRD task should establish this)
- Lint/format: `<e.g. ruff check .>`
- Build/run: `<command>`

## Conventions

- Language/stack: <TBD>
- Commit style: one task per commit, imperative message, reference the PRD task.
- Structure: <TBD>

## Known constraints

<Anything the agent must not touch or assumptions it must hold.>
